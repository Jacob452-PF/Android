<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Bluetooth clásico -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Ubicación -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- BLE -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_Panchito_Explorer">

        <activity
            android:name=".VideoActivity"
            android:exported="false" />

        <activity
            android:name=".ObjetivosActivity"
            android:exported="false" />

        <activity
            android:name=".AsercaDeLaAppActivity"
            android:exported="false" />

        <activity
            android:name=".manualActivity"
            android:exported="false" />

        <activity
            android:name=".importarMapaActivity"
            android:exported="false" />

        <activity
            android:name=".verMapaGuardadoActivity"
            android:exported="false" />

        <activity
            android:name=".guardarMapaActivity"
            android:exported="false" />

        <activity
            android:name=".mapeoRealActivity"
            android:exported="false" />

        <activity
            android:name=".editarDispositivoActivity"
            android:exported="false" />

        <activity
            android:name=".antesDeMapearActivity"
            android:exported="false" />

        <activity
            android:name=".dispositivoActivity"
            android:exported="false" />

        <activity
            android:name=".guardadosActivity"
            android:exported="false" />

        <activity
            android:name=".bluetoothActivity"
            android:exported="false" />

        <activity
            android:name=".homeActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>